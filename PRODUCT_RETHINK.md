# 言控 · 产品重构思考（Jobs 式）

> 不是“功能更多”，而是“每一次打开都更接近目标”。

## 1. 重新定义产品

言控不是“带 Agent 的自动化工具”，也不是“手机控制台”。

它应该是一个：

> **你开口，它完成。**
>
> 打开就是对话，对话就是能力，能力就是结果。

当前产品最大的问题：
- 用户打开后看到的是“会话列表 / 权限提醒 / 设置入口”，而不是“我能做什么”。
- 自动化页像开发者任务表格，不像生活助理。
- 设置页像设备管理后台，不像个人助理的个人中心。
- 技术能力（SSH、远程节点、Token）被放在和“喝水提醒”同级的可见位置。

## 2. 产品原则（不可妥协）

1. **第一眼必须能说话**
   - 打开 App，输入框应在屏幕下半部分稳定存在。
   - 不是先找“新建会话”，也不是先看权限。

2. **配置永远退后**
   - 未配置大模型：只显示一条细状态条，不打断。
   - 未开启无障碍：不显示大段说明，只在相关任务失败时出现。
   - API Key、Token 不直接展示给普通用户，显示“已配置/未配置”。

3. **功能是会话，不是菜单**
   - 点咖啡、查母婴、控制空调、跑 SSH，都是“说一句话”的结果。
   - 页面可以作为结果视图，但不应成为导航中心。

4. **自动化是承诺，不是配置**
   - 任务卡片应展示：
     - “每天早上 8 点打开企业微信”
     - “下次：明天 08:00”
     - “上次：成功”
   - 不应默认展示包名、Deep Link、JSON。

5. **高级能力必须“高级”**
   - 远程节点、SSH、审计、调试、原始日志全部放到“开发者模式”之后。
   - 普通用户永远看不到。

6. **少即是多**
   - 一个页面只解决一个用户目标。
   - 宁可功能隐藏，不可界面喧闹。

## 3. 信息架构 V2

```text
底部导航（3 个）

[对话]            [自动化]          [我的]

对话
├─ 空状态：大输入框 + 3 个示例
├─ 会话历史：轻量入口，不占主屏
├─ 输入区：永远在底部，左侧麦克风 + 文本 + 发送
└─ 快捷能力：仅在空会话时出现

自动化
├─ 顶部：自动化任务
├─ 主按钮：让言控创建一个自动化
├─ 任务卡片：人类可读 + 下次执行 + 上次结果
└─ 模板：仅从“更多”进入

我的
├─ 状态：模型 / 语音 / 家庭 / 远程
├─ 通用：通知 / 语言 / 外观
├─ 能力：语音 / Home Assistant / 购物
├─ 高级：远程 / SSH / 审计 / 调试（默认折叠）
└─ 开发者模式：显式开启后才显示高级
```

## 4. 关键页面重构

### 4.1 对话页
- 顶部：`言控` + 状态点 + 历史按钮
- 未选择会话时：
  - 大标题：“今天想让我做什么？”
  - 示例卡片 3 个，不铺满
  - 底部固定输入框
- 选择会话后：
  - 顶部显示会话标题
  - 消息区占满
  - 工具步骤内联
  - 底部输入框永远存在

### 4.2 自动化页
- 移除“任务预览 / JSON / 包名”级别的默认展示。
- 新建入口：
  - “让言控创建” → 回到对话并自动填入自然语言
- 手动高级编辑放入任务详情中。

### 4.3 我的页
- 不直接编辑 API Key。
- 显示卡片：
  - AI 模型：已配置 / 未配置
  - 自动化权限：已就绪 / 需设置
  - Home Assistant：已连接 / 未配置
  - 语音唤醒：已开启 / 未开启
- 点击“AI 模型”才展开 Key / Model 编辑。

### 4.4 Onboarding
- 第一步：欢迎 + 一句话示例
- 第二步：真正请求通知权限和麦克风权限
- 第三步：检测无障碍/Shizuku，不阻塞
- 第四步：完成

## 5. 实现架构建议

### 当前问题
- `MainActivity` 仍是巨型 Compose 文件。
- 页面切换用 `HorizontalPager + boolean overlay`，不是路由。
- 设置、购物、HA 全部绑定同一个 `MainViewModel`。
- 大量 UI 状态散落在 Composable 内。

### 目标架构

```text
App
└── VoiceConfigRoot
    ├── OnboardingGate
    └── MainScaffold
        ├── Destination.Conversation
        ├── Destination.Automation
        └── Destination.Profile
            ├── ShoppingPage
            └── HomeAssistantPage
```

- 引入 `Navigation Compose` 或至少 sealed class + `AnimatedContent`。
- 每个 feature 一个 Screen + 一个 ViewModel：
  - `ConversationViewModel`
  - `AutomationViewModel`
  - `ProfileViewModel`
  - `ShoppingViewModel`
  - `HomeAssistantViewModel`
- 建立 UI 组件库：
  - `VoiceStatusBar`
  - `VoiceEmptyState`
  - `VoiceCommandBar`
  - `VoiceSectionCard`
  - `VoiceTaskCard`
- `MainViewModel` 只保留跨功能协调，不再负责 SSH/HA/购物展示。

## 6. 应该砍掉或深藏的东西

- 对话输入区内的所有语音开关 → 已移入“我的”，但未来只保留一个“语音”总开关。
- 首页权限大卡片 → 改为一条状态条。
- 设置页 API Key 明文 → 改为“已配置 / 点击编辑”。
- SSH / 远程 / 调试 → 开发者模式。
- 成功率百分比 → 已移除，继续保持。
- Emoji 功能图标 → 已基本清理，继续推进。

## 7. 验收标准

1. 新用户 30 秒内能说出第一个真实指令。
2. 未配置任何东西时，主界面依然是一个完整、干净的对话。
3. 配置类内容在普通用户路径中出现次数为 0。
4. 自动化任务卡片不需要懂任何技术名词即可理解。
5. 普通用户找不到 SSH、远程节点、调试日志。
6. 每次打开 App，用户都知道下一步做什么。
