# 言控 VoiceConfig UI/UX 详细重构设计

> 版本：V1.0  
> 日期：2026-08-28  
> 视角：产品经理 + 体验设计  
> 目标：把“开发者工具/极客自动化面板”重新设计成“可靠、克制、有温度的个人生活与工作智能助手”

---

## 1. 产品原则

### 1.1 一句话定位

> 用户说一句话，言控负责理解、规划、执行、验证，并在安全边界内完成数字生活中的重复、繁琐、多步任务。

### 1.2 设计原则

1. **对话优先**
   - 首页和主 Tab 的第一主角是“用户目标 + 执行结果”，不是配置项。

2. **技术退后**
   - Shizuku、ADB、SSH、密钥、节点等全部视为“高级能力”，不得出现在主路径。

3. **引导代替说教**
   - 权限/能力缺失用“轻量状态条 + 一键引导”，不要用长文本技术说明占屏。

4. **安全可见但不过度打扰**
   - 敏感操作必须确认，但确认卡应克制、明确、可撤销。

5. **专业但不冷**
   - 视觉要有 AI 产品的现代感、可信感，同时保留生活助手的温度。

6. **每个页面都有下一步**
   - 空状态、失败状态、等待状态都必须告诉用户“现在可以做什么”。

---

## 2. 信息架构与导航

### 2.1 一级导航

```text
底部 Tab Bar（3 个）
├─ 首页 / 对话
│   默认：最近会话 + 快捷任务 + 今日建议
│
├─ 自动化
│   默认：我的任务 + 新建任务 + 最近执行
│
└─ 我的 / 设置
    默认：账户与服务状态
    高级：AI / 语音 / Home Assistant / 远程节点 / SSH / 调试
```

> 当前顶部 Tab 的问题：把“自动化”和“智能助手”放在顶部，缺少全局主导航。
> 新设计改为底部导航，更符合单手操作和现代手机 App 习惯。

### 2.2 二级页面

```text
首页/对话
├─ 会话列表
├─ 会话详情（聊天 + 步骤时间线）
├─ 新建会话
├─ 运行记录 / 审计
└─ 任务计划详情

自动化
├─ 任务列表
├─ 新建任务
├─ 编辑任务
├─ 执行记录
└─ 模板库

我的 / 设置
├─ 个人信息与状态
├─ 通用设置
├─ AI 与模型
├─ 语音
├─ Home Assistant
├─ 远程设备
│   ├─ 节点管理
│   ├─ SSH 文件
│   ├─ SSH 终端
│   ├─ 系统服务
│   ├─ 审计
│   └─ 密钥
├─ 权限与系统
└─ 高级/调试
```

### 2.3 导航原则

- 主操作固定：首页/自动化/我的。
- 配置类页面统一从“我的”进入。
- 远程 SSH 等高级工具全部放在“高级工具”分组，不直接暴露在一级页面。

---

## 3. 视觉设计系统

### 3.1 品牌调性

```text
关键词：可靠 / 克制 / 有温度 / 智能 / 不炫技
```

### 3.2 色彩

| Token | 色值 | 用途 |
|---|---|---|
| Primary | `#4F46E5` | 主按钮、选中态、品牌主色 |
| Primary Container | `#E0E7FF` | 浅色背景、选中卡片 |
| Secondary | `#7C6FDE` | 次级强调、图表 |
| Accent Warm | `#F5B84D` | 提醒、奖励、温馨提示 |
| Success | `#16A34A` | 成功、已完成 |
| Warning | `#D97706` | 警告/需要关注 |
| Error | `#DC2626` | 错误/危险 |
| Surface | `#FAFAFC` | 页面背景 |
| Surface Card | `#FFFFFF` | 卡片背景 |
| Text Primary | `#1E1B2E` | 主文字 |
| Text Secondary | `#6B7280` | 次要文字 |

> 不再使用 Material 默认 `#6750A4` 作为品牌色，改用更有辨识度的深靛蓝。

### 3.3 字体

- 中文：系统默认黑体优先，建议 `MiSans / HarmonyOS Sans / PingFang SC`。
- 英文/数字：`Roboto / Inter`。
- 标题：`22sp / SemiBold`
- 正文：`16sp / Regular`
- 辅助：`13sp / Regular`
- 标签：`12sp / Medium`

### 3.4 圆角

```text
大卡片：20dp
中卡片：16dp
按钮：12dp
输入框：14dp
气泡：18dp
```

### 3.5 间距

```text
页面边距：16dp
卡片间距：12dp
组内间距：8dp
元素间距：6dp
```

### 3.6 图标

- 全部使用统一线性图标（Material Symbols Outlined 或自绘图标）。
- 禁止使用 Emoji 作为功能图标。
- 主操作使用实心图标，次级操作使用线性图标。
- 图标尺寸：
  - 导航：24dp
  - 列表：22dp
  - 卡片标题：20dp
  - 小按钮：18dp

### 3.7 Logo 方向

建议采用：

```text
主形：一个圆角方形
内部：抽象“言”字 + 声波 / 对话气泡 + 完成弧线
寓意：用户说话，言控完成
```

可提供 3 个方向让用户选择：

1. 言字 + 声波
2. 对话气泡 + 对勾/星
3. 抽象语音星 + 任务完成环

---

## 4. 全局组件库

### 4.1 顶部导航

```text
[← 返回]  页面标题                 [主操作]
```

- 标题左对齐，主操作右对齐。
- 返回按钮仅在二级页面出现。

### 4.2 底部导航

```text
[首页/对话]   [自动化]   [我的]
```

- 选中态：Primary 图标 + 文字。
- 未选中态：灰色线性图标 + 文字。

### 4.3 卡片

- 圆角 16dp。
- 背景 `#FFFFFF`，阴影极浅（`0 2 8 rgba(0,0,0,0.06)`）。
- 卡片内边距 16dp。

### 4.4 主按钮

```text
[ + 新建任务 ]
```

- 高度 48dp。
- Primary 背景。
- 圆角 12dp。

### 4.5 状态条 / 轻提示

```text
⚠️ 还差 2 项设置即可自动执行任务     [去完成]
```

- 默认不高亮，使用浅色 Surface。
- 点击后进入引导页。

### 4.6 空状态

```text
[插图]
还没有会话
输入一句话，言控会帮你完成
[开始一个新任务]
```

### 4.7 加载与进度

- 页面加载：顶部细进度条。
- 长任务：任务卡片显示百分比/步骤进度。
- 语音等待：输入框左侧声波动画。

### 4.8 敏感操作确认

```text
─────────────────────────────
确认执行
将发送微信回复：
“好的，我明天准时参加。”

[取消]  [确认并执行]
─────────────────────────────
```

- 必须显示具体动作内容。
- 不可逆操作使用红色确认按钮。
- 自动确认开关只能影响“低风险敏感操作”，不能绕过“不可逆操作”。

---

## 5. 首次启动 / Onboarding

### 5.1 目标

让用户 30 秒内理解“这是什么”，并完成最小可用配置。

### 5.2 流程

```text
Step 1 欢迎页
  标题：你好，我是言控
  文案：你的一句话，我帮你完成
  示例卡片：
    - 每天早上 8 点打开企业微信
    - 帮我点一杯瑞幸
    - 帮我查母婴用品并比价
  [开始使用]

Step 2 基础权限
  通知权限：需要，用于任务结果提醒
  麦克风权限：可选，用于语音输入/唤醒
  [下一步]

Step 3 能力检测
  检测无障碍/Shizuku
  - 如果未开启：
    显示“高级自动化未开启，基础提醒和定时任务仍可使用”
    提供“去开启 / 稍后再说”
  - 如果已开启：
    显示“自动化能力已就绪”

Step 4 完成
  “你可以开始了”
  [进入首页]
```

### 5.3 原则

- 不要把 Shizuku/ADB 放在主线。
- 未开启时，不阻塞提醒、定时、简单 Agent 对话等基础能力。
- 只把“需要读屏/点击”的复杂任务标记为“需要高级权限”。

---

## 6. 首页 / 对话首页

### 6.1 页面结构

```text
顶部：品牌区
  言控
  你的个人生活与工作智能助手

轻量状态条（仅在有未完成事项时出现）
  ⚠️ 还差 2 项设置     [去完成]

快捷任务区
  [打开企业微信] [点瑞幸] [回复微信] [查母婴用品]
  [查看更多]

今日建议 / 常用任务
  根据时间/历史推荐
  - 工作日 08:00 打开企业微信
  - 10:00 提醒喝水
  - 18:02 下班打卡

最近会话
  今天
    [会话卡片]
  昨天
    [会话卡片]

底部导航
  [首页/对话] [自动化] [我的]
```

### 6.2 会话列表卡片

```text
┌──────────────────────────────────┐
│ 打开企业微信                      │
│ 16 条消息 · 今天 08:00            │
│ 执行结果：✅ 已打开                │
│                          [⋯]     │
└──────────────────────────────────┘
```

- 卡片高度约 84dp。
- 显示最后一条结果摘要。
- 长按/更多菜单：重命名、删除、归档。

### 6.3 空状态

```text
还没有会话
你可以试试：
  “每天早上 8 点打开企业微信”
  “帮我对比三款婴儿奶粉”
[开始一个新任务]
```

### 6.4 反馈

- 点击快捷任务会直接创建/填入会话，而不是跳配置页。
- 若缺少权限，快捷任务仍可进入，但显示“高级能力未开启”的轻提示。

---

## 7. 会话详情页（Agent 聊天）

### 7.1 布局

```text
顶部：返回  会话标题    [⋯]
──────────────────────────────────
[对话内容区，占满剩余空间]
  用户消息（右侧气泡）
  Agent 消息（左侧气泡）
  工具执行步骤时间线
  等待确认卡片
──────────────────────────────────
输入区：
  [🎤]  [输入一句话...]        [发送]
──────────────────────────────────
```

### 7.2 对话气泡

- 用户：右侧 Primary Container 背景。
- Agent：左侧白色卡片背景。
- 工具调用：小号等宽字体，显示工具名和状态。

### 7.3 执行时间线

```text
✅ 打开企业微信
   08:00:12  ·  427ms

✅ 读取界面
   08:00:13  ·  212ms

⏳ 正在确认订单...
```

- 可展开查看参数、截图、证据。
- 失败步骤红色高亮。

### 7.4 等待确认卡片

```text
需要你确认
言控准备发送微信回复：
“好的，我明天准时参加。”

[取消] [确认发送]
```

### 7.5 输入区

- 麦克风按钮在输入框左侧。
- 语音动效只在录音时出现。
- 不展示“语音自动发送 / TTS / 唤醒”开关；这些移入设置。

### 7.6 语音唤醒

- 需要麦克风权限时，在输入框上方显示一次性提示：
  ```text
  开启语音唤醒后，即使不打开 App，也能说“言控”唤醒
  [去开启] [暂不]
  ```
- 开启后不再常驻。

---

## 8. 自动化页

### 8.1 页面结构

```text
顶部：自动化              [搜索] [模板]
──────────────────────────────────
[+ 新建任务]（主按钮）
──────────────────────────────────
我的任务（N）
  分组：全部 / 工作中 / 已暂停 / 已结束
  [任务卡片]
──────────────────────────────────
最近执行
  [执行记录卡片]
  [查看全部]
──────────────────────────────────
```

### 8.2 任务卡片

```text
┌──────────────────────────────────┐
│ 打开企业微信                      │
│ 每天 08:00 · 下次 08/29 08:00     │
│ 上次：✅ 成功                     │
│                    [开关] [⋯]    │
└──────────────────────────────────┘
```

- 开关：可直接启用/停用。
- 更多菜单：编辑、立即执行、暂停、删除。

### 8.3 新建任务

- 表单式，步骤少。
- 支持：
  - 简单任务（提醒、打开 App、打开链接）
  - Agent 任务（输入一句话，交给言控）
- 提供模板选择：
  - 打开企业微信
  - 喝水提醒
  - 点瑞幸
  - 每周工作总结

### 8.4 执行记录

- 去掉“成功率 13%”这类负面数字。
- 改为：
  ```text
  最近执行
  今天 08:00 打开企业微信  ✅ 成功
  今天 07:00 提醒喝水      ⚠️ 未执行
  ```
- 可展开查看失败原因。

---

## 9. 我的 / 设置页

### 9.1 页面结构

```text
我的
├─ 账号与状态
│   模型：已配置
│   自动化权限：未开启
│   Home Assistant：未配置
│   语音唤醒：未开启
├─ 通用
│   外观 / 语言 / 通知
├─ 智能助手
│   自动确认 / 自动验证 / TTS / 唤醒词
├─ 语音
│   识别引擎 / 唤醒词 / 语音播报
├─ 家庭与设备
│   Home Assistant
│   智能设备
├─ 高级能力
│   远程节点
│   SSH 文件 / 终端 / 服务 / 审计 / 密钥
│   远程项目
└─ 高级 / 调试
```

### 9.2 每部分建议

- 使用分组卡片。
- 每组显示当前状态摘要。
- 远程相关全部放入“高级能力”，默认折叠。

### 9.3 模型与密钥

```text
AI 模型
  状态：已配置
  模型：deepseek-v4-flash-vision-exp
  [更换模型]
```

- 不直接显示密钥明文。
- 显示“已配置/未配置”。

### 9.4 语音设置

```text
语音唤醒
  状态：未开启
  唤醒词：言控 / 你好言控
  说明：需要麦克风权限
  低功耗模式（实验性）
```

---

## 10. Home Assistant 页面

### 10.1 入口

- 从“我的 → 家庭与设备 → Home Assistant”进入。

### 10.2 页面

```text
Home Assistant
状态：已连接 / 未连接
[ Base URL ]  [ Token ]
[测试连接]

设备
  客厅空调  26°C  制冷  [控制]
  卧室灯    开        [开关]
  窗帘      关闭      [打开]
  电视      关闭      [打开]
```

### 10.3 控制面板

- 低风险设备直接控制。
- 门锁/摄像头/安防弹确认卡。
- 控制成功后显示轻提示。

---

## 11. 远程设备 / SSH（高级能力）

### 11.1 入口

- 全部收进“高级能力”，默认折叠。

### 11.2 节点管理

```text
远程节点
[+ 添加节点]
节点卡片：
  makapi · 在线
  192.168.31.110:8787
  最近在线：刚刚
  [执行] [停用] [编辑]
```

### 11.3 SSH 文件

- 全屏文件管理器。
- 顶部路径栏，支持返回上级、刷新。
- 文件列表使用统一图标，不用 Emoji。

### 11.4 SSH 终端

- 全屏终端。
- 顶部显示连接信息。
- 支持复制、清屏、发送 Ctrl+C。

---

## 12. 购物研究页面

### 12.1 入口

- 从对话或首页快捷任务进入。
- 也可以从“我的 → 智能能力”进入。

### 12.2 页面

```text
购物研究
[输入目标：婴儿奶粉 3-6 个月，预算 300 元]
[开始研究]

研究进度
  ✅ 搜索
  ✅ 提取信息
  ✅ 价格比较
  ⏳ 生成推荐

推荐清单
  1. 商品A 京东 ¥199  4.8分 [加入清单]
  2. 商品B 淘宝 ¥179  4.7分 [加入清单]

清单状态：WATCH / RECOMMENDED / BOUGHT
```

---

## 13. 全局状态设计

### 13.1 加载

- 首次进入：骨架屏。
- 任务执行：步骤时间线 + 进度。
- 语音识别：输入框旁声波动画。

### 13.2 空状态

每个页面都有“下一步”：
- 会话空 → 开始新任务
- 自动化空 → 新建任务
- 购物清单空 → 开始研究
- 远程节点空 → 添加节点

### 13.3 错误状态

- 使用友好的中文，不用错误码吓用户。
- 尽量提供重试/换方式。

### 13.4 敏感操作

- 所有发送/支付/删除必须确认。
- 确认卡展示具体内容。
- 不可逆操作使用红色按钮。

---

## 14. 设计验收清单

- [ ] 首屏是否 30 秒内看懂产品价值
- [ ] 配置项是否全部不在主路径
- [ ] 对话区是否占满主屏
- [ ] 是否无 Emoji 功能图标
- [ ] 是否使用 Adaptive Icon
- [ ] 空状态/错误状态是否有下一步
- [ ] 敏感操作是否有清晰确认
- [ ] 高级能力是否默认折叠
- [ ] 视觉是否统一、专业、有温度
- [ ] 是否支持单手操作和底部导航

---

## 15. 开发优先级

```text
P0
1. 首页/会话页信息层级重构
2. 3
步 Onboarding
3. 技术配置收进高级设置
4. 底部导航

P1
5. 图标系统替换 Emoji
6. 语音开关移入设置
7. 自动化新建任务主按钮
8. 执行记录展示优化

P2
9. Adaptive Icon 与品牌色
10. Home Assistant / 购物研究完整页面
11. 语音唤醒引导
12. 远程高级能力统一设计
```

---

## 16. 验收故事

### 故事 1：新用户

```text
用户第一次打开言控
→ 看到“你好，我是言控”
→ 点击“开始使用”
→ 授权通知/麦克风
→ 进入首页
→ 首页显示快捷任务和空会话
```

### 故事 2：定时打开企业微信

```text
用户在首页点击“打开企业微信”
→ 进入会话
→ 输入“每天早上8点打开企业微信”
→ 言控创建定时任务
→ 自动化页出现任务卡片
→ 到点执行并通知
```

### 故事 3：回复微信

```text
用户说“帮我回复老板明天会参加”
→ 言控读取微信消息
→ 生成草稿
→ 展示确认卡
→ 用户点击“确认发送”
→ 言控发送并反馈
```

### 故事 4：语音唤醒

```text
用户开启语音唤醒
→ 锁屏状态下说“言控”
→ 言控打开首页
→ 用户继续说“帮我点瑞幸”
→ 言控开始执行
```
