# VoiceConfig 系统性思考与战略复盘

> 更新：本 session 之后
> 目的：把“个人生活/工作数字 Agent”从功能堆叠，收敛为可验证、可复现、可安全托底的系统。

---

## 1. 终极目标

VoiceConfig 的终极目标不是“做一个会聊天、会读屏的 App”，而是：

> **成为用户可信赖的个人数字执行 Agent：**
> 通过语音 / 文字 / 触发器自然输入，理解意图，制定可跟踪计划，
> 在安全边界内执行真实手机与数字生活操作，完成到“只差用户最后确认/操作”的最后一步，
> 并且每一步都有证据、可审计、可失败、可恢复、可复现。

关键判断标准：

- **不是“准备好了”**，而是“已经做到用户只差最后一步”：
  - 瑞幸：稳定停在免密支付页，且没有浮层遮挡。
  - 微信：草稿已填入并停在发送确认，等待用户点发送。
- **不是“看起来成功”**，而是“有可观察证据”：
  - 前台包名、关键 UI 文本、关键元素不存在、未执行最终动作、trace/截图完整。
- **不是“模型自律”**，而是“确定性系统兜底”：
  - 安全门、工具层、验证层都不依赖 LLM 的自觉。
- **不是“单点可用”**，而是“长期可靠”：
  - 重启、杀进程、MIUI 回收、无障碍掉线、Shizuku 缺失、锁屏、网络抖动都能降级或恢复。

---

## 2. 本轮 Session 发现/确认的技术债

### 2.1 真机执行通道仍是最脆弱的地基
- 无障碍服务在系统层显示 Bound，但 App 进程内 `instance` 可能仍为 null。
- 安装/更新 APK 后服务不一定自动重连，MIUI 下需要人工/系统重启才稳定。
- 真机可能离线、锁屏，自动化无法自愈。
- Shizuku 缺失时，读屏、截图、shell 输入全部降级，只剩无障碍。

### 2.2 UI 操作曾缺少统一原语
- 已新增 `UiActionLayer`，但还不是“所有工具都走这一层”。
- 仍存在重复的读树、坐标解析、Shizuku 兜底逻辑。
- 坐标点击仍然是最后兜底，但“最后兜底”不能成为常见路径。

### 2.3 终止条件此前不严谨
- 已新增 `TerminalSafetyGate`，但尚未在真机完成最终验证。
- 支付/发送/删除/配置修改等不同域的“终端态”需要完整矩阵，不能只覆盖瑞幸和微信。
- 需要统一“终端页特征”与“允许继续/必须等待”的映射。

### 2.4 E2E 验证仍偏“结果 ok”
- 已增强脚本支持 `expectedForeground / terminalText / absentText / requireWaiting`。
- 但还没有跑通真机严格场景。
- 需要把 trace、截图、最终动作未执行、浮层缺失、等待状态都变成自动断言。

### 2.5 模型行为不稳定
- 重复读屏、猜坐标、长篇推理、同一流程每次路径不同。
- LLM 适合做“理解与规划”，不适合做“像素级点击器”。
- 当前仍让 LLM 在 UI 树/坐标上自由发挥，导致失败面大。

### 2.6 缺少确定性技能/复放路径
- 已有 Skill 基础设施，但 Luckin / WeChat / WeCom / HA / Remote 还没有沉淀为“可审核、可复放、可验证”的确定性 Skill。
- 每次执行都从零让模型探索，是 E2E 不稳定和耗时的主要来源。

### 2.7 弹窗/浮层识别仍是启发式
- `close_iv` 已修，但其他 App 的营销层、权限层、功能选择层没有统一处理。
- 需要把“可关闭层 / 不可关闭功能层 / 系统权限层 / 终端确认层”分类成确定性规则。

### 2.8 本地语音/低功耗尚未完成验收
- KWS/ASR 组件已接入，但缺少 1 小时连续唤醒、低电量、灭屏/亮屏切换的真实耗电与稳定性数据。

### 2.9 安全与人工确认的“最后一公里”还不够
- 安全硬拦截已存在，但语音确认、Agent 层二次确认、终端人工操作三者的关系还需要完整交互设计。
- 不可逆操作必须永远无法被“自动确认”绕过；终端态必须永远等用户实际操作。

### 2.10 本轮真机 E2E 后的新认知（2026-08-30）
- 真机严格终端 E2E 已达到 2/2 PASS：瑞幸停在免密支付，微信停在 Send 前。
- 但这也暴露新债：
  - **微信等 App 不向无障碍暴露可读节点**，`read_ui/get_screen_state` 无法工作，只能靠 `read_screen` 视觉截图。
  - 视觉截图使模型 token/上下文急剧膨胀，且模型仍可能在“已有目标消息”时过早回复完成。
  - 微信文字输入最终靠“无障碍粘贴”成功，但这不是通用输入通道；不同 App/输入法/安全窗口仍可能失败。
  - `TerminalSafetyGate` 在不可读 UI 下需要借助模型最终文本识别 Send，虽然只用于“等待用户”（安全方向），但还不能作为完美证据。
  - E2E 的 `terminalText` 在微信使用了 message fallback，未来应补充截图存档/人工抽检/OCR 证据。
  - 全量套件中瑞幸稳定、微信仍受模型行为影响，必须用 Skill 固化路径。

### 2.11 平台合规与账号风控（本轮重大新认知）
- 个人微信没有官方个人号自动化 API；腾讯明确禁止模拟器、Hook、插件、脚本自动操作。
- 使用模拟器 + 无障碍自动打开/输入微信，已经触发真实账号“安全使用提醒”。
- 结论：**个人微信自动化不能成为产品主路径**，只能作为“小号 + 人工 + 低频”的受限实验。
- 技术上已落地：`WechatRiskGuard` 默认禁用个人微信 UI 自动化；`wecom_send_message` 走企业微信官方 API。

### 2.12 执行速度与上下文膨胀
- 真机 trace 显示截图全量进入 LLM 历史后，单次请求可到 **10MB+**，单轮等待 10～35 秒。
- 已修复：只保留最近 2 张截图、截图默认最长边 1440、JPEG 质量 80、视觉读屏预算、completion check 降为 1 次。
- 已补 TraceReportBuilder（耗时/工具序列/截图/验证/安全拦截/失败原因）；仍缺修复后的真机正式耗时基线和 token 统计。
- LLM 自由探索仍会绕路；Skill 驱动是下一阶段根治。

### 2.13 官方 API 替代仍不足
- 已新增企业微信官方 API 发送工具与设置页。
- 但仍没有：
  - 企业微信测试号真实联调（设置页已加凭证测试按钮）
  - 微信客服/公众号/小程序 API
  - 官方 API 的凭证安全存储、权限最小化、调用审计
- 个人微信能力必须持续避免，不能让 UI 自动化重新成为默认路径。

### 2.14 模拟器与真机验证需要分离
- 已新增 Mock LLM 使模拟器无需 API Key 也能跑 Agent 链路。
- 模拟器适合验证：工具注册、UI 原语、截屏体积、安全拦截、状态机。
- 真机仍必须验证：MIUI 无障碍自愈、真实 App 版本差异、锁屏/离线、真实 API 网络。
- 不能把模拟器 Mock 结果等同于真实产品验收。

---

### 2.15 本轮后续增量（2026-08-30 模拟器推进）
- **真实工具失败必须暴露**：
  - 之前 `wecom_send_message` 缺配置时工具失败，但 Agent 仍可能回复“完成”；
  - 已修复：只要真实工具执行失败，Agent 强制 FAILED，并把失败原因写入最终消息。
- **模拟器离线回归扩展到 11/11**：
  - 覆盖打开设置、读 UI、截屏、读屏压力、等待确认、弹窗、返回键、ui_assert、个人微信风控、企业微信/HA 未配置；
  - 证明安全门和失败传播在模拟器上是确定的。
- **可观测性继续加强**：
  - TraceReport 已包含耗时、工具序列、截图数、验证数、安全拦截、LLM 错误、Token、请求体、失败分类；
  - 可按 runId 导出 Markdown。
- **智能家居/树莓派进入“可接入但未联调”状态**：
  - 代码已支持 HA `home_devices` / `home_control` 和远程 SSH/节点；
  - 但没有真实 HA Token、真实树莓派 SSH、真实海信空调/窗帘设备；
  - 用户已完成设备采购/改造方向调研，尚未形成可复放的真实 Skill。
- **低成本 DIY 智能家居带来新约束**：
  - 50 元预算可以开始做单轴/单设备试验，但不能作为“稳定产品能力”承诺；
  - 需要用 ESPHome/Home Assistant/官方网关等确定性路径，避免为省钱引入不可控私有协议。

## 2.16 本轮真实 LLM 性能优化后的技术债（2026-08-30 晚）

- **性能已大幅优化，但“快”不能代替“稳”**：
  - 关闭 DeepSeek 思维链后真机全流程约 80s；
  - 新增 `luckin_quick_order` 宏后，干净购物车实测 **11.7s / 3 次工具调用**；
  - 代价是：复杂/异常页面下模型少了推理，可能更容易走错或需要回退；
  - 需要“按域/按风险”动态决定是否开启思维链，而不是全局关死。

- **第一版宏是硬编码状态机，不可维护**：
  - 已重构为 `FlowScript` 数据 + `UiFlowExecutor` 通用执行器；
  - 瑞幸流程已沉淀为 `BuiltinFlowScripts.luckinStandardIce`；
  - 但仍存在新债：
    - FlowScript 目前还是 Kotlin 数据，不是可导入/可版本化的 JSON；
    - 步骤/候选词/门店名仍带具体文案，跨门店/跨版本需要持续维护；
    - 没有 FlowScript 的 schema 校验、版本迁移、灰度/回滚；
    - 没有“从成功 trace 自动生成候选 FlowScript”的闭环；
    - `UiFlowExecutor` 还没有完整的每步断言/失败原因分类。

- **宏只覆盖瑞幸单域，通用性未证明**：
  - 企业微信、HA、远程项目都还没有对应的 FlowScript；
  - 需要先建立“FlowScript 标准 + 测试框架”，再逐个领域复制；
  - 不能把瑞幸成功当成“所有 App 都能这么干”。

- **购物车/历史状态导致测试不确定性**：
  - 宏能快速完成，但真实购物车可能残留其他商品；
  - 当前没有“下单前清空/校验购物车”的通用前置步骤；
  - 需要把“前置条件检查”纳入 FlowScript，而不只是“从当前界面直接开始”。

- **安全与速度仍需保持冲突最小化**：
  - 宏中禁止支付/提交的边界必须由数据/引擎强制，不能依赖脚本编写者自觉；
  - 后续要把“禁止动作”作为 FlowScript 的一等字段，由引擎统一校验。

- **调试广播仍是开发专用**：
  - `DEBUG_THINKING / DEBUG_AUTO_VERIFY_MAX` 方便真机调参；
  - 生产应通过设置页/能力状态统一管理，不能只有 adb 调试口。

- **熄屏定时任务缺少唤醒/解锁通道**：
  - 真机实测：熄屏 + 仅无障碍时，`open_app` 尝试打开企业微信但无法验证前台，运行失败；
  - 原因：无障碍不能操作灭屏界面；无 Shizuku 时只能走 `context.startActivity`，受后台启动限制且没有 `PowerManager.wakeup` / 解锁步骤；
  - 需要明确策略：有 Shizuku 时使用 `am start + keyevent 224(+dismiss-keyguard)`；无 Shizuku 时降级为高优先级通知/全屏提醒，等待用户点亮后继续。

## 2.17 从“会跑”到“可持续”的关键转变

- 之前：每次都是“LLM 自由探索 → 偶然成功 → 记录 Skill 文本”。
- 现在：已知流程应变成 **可执行、可验证、可审计的 FlowScript**。
- 之后：新流程应先生成候选脚本，再经真机回归、人工审核、版本化后 APPROVED。
- 最终：LLM 只负责“理解/选择/异常兜底”，确定性引擎负责“稳定执行”。

## 2.18 本轮 FlowScript 平台化 / 熄屏强提醒后的新认知（2026-08-30 续）

- **FlowScript 已从“想法”变成真平台底座**：
  - JSON 格式 + schema 文件 + 导入导出/校验/版本/审核状态
  - `FlowScriptStore` 管理内置与自定义脚本，导入默认 PENDING 需人工审核
  - `run_flow_script` 通用工具绕过专用状态机，`UiFlowExecutor` 统一执行
  - 参数模板 `{drink}` / `{temperature}` 已让瑞幸脚本可被 LLM/工具覆盖
  - `InputText` / `Wait` 动作补上，脚本表达能力开始覆盖“点、等、输”
  - 执行结果带 `elapsedMs / iterations`，可做性能预算
  - 设置页已能导入、审核、启停、导出流程

- **但仍只是“单域成功”，多域未被证明**：
  - 内置 FlowScript 仍然只有瑞幸；
  - 企业微信、HA、远程项目、日历提醒等域还没有确定性 FlowScript；
  - 最大的下一步不是继续加功能，而是**把同一个标准复制到第二个域并跑通真机 golden**。

- **新债 1：FlowScript 生命周期不完整**：
  - 有 PENDING/APPROVED/REJECTED，但没有“从成功 trace 自动生成候选脚本”；
  - 没有版本迁移/回滚/差异对比；
  - 没有每个脚本的运行历史、成功率、失败分类；
  - 导入审核目前是人工在设置页操作，缺少 CLI/自动化审核桥。

- **新债 2：参数化仍是“字符串替换”**：
  - `{key}` 只是文本模板，没有类型、枚举、默认值、必填校验；
  - 脚本如果引用未声明参数，只能靠静态 golden 测试兜底；
  - 不同用户/门店/设备需要配置化默认参数，目前没有用户级参数层。

- **新债 3：熄屏降级有了“提醒”，还没有“续跑闭环”**：
  - `strong_remind` 已落地并接入定时任务失败/等待且熄屏时自动强提醒；
  - 但用户点亮屏幕后，缺少“自动恢复原任务并继续执行”的状态机；
  - 需要把任务标记为 `WAITING_USER_WAKE`，用户在通知/首页确认后恢复 Agent 当前计划。

- **新债 4：执行模式已定义，但还没有真正驱动调度**：
  - `ASSIST / AMBIENT / NOTIFY` 已进入能力状态；
  - 目前调度器还是“失败后强提醒”的被动降级；
  - 应变成主动路由：Shizuku 可用 → Assist；仅无障碍 → Ambient（要求亮屏）；无 UI 通道 → Notify。

- **新债 5：安全仍是“引擎拒绝”，不是“全流程证据链”**：
  - `forbiddenActionTokens` 已由执行器强制；
  - 但还没有“每个 FlowScript 的终端人工确认 UI”统一组件；
  - 没有把“最终动作未执行”作为强制 trace 断言；
  - 导入脚本的风险分级、来源可信度、提交人审计还没有。

- **新债 6：测试仍缺少“真机 golden”**：
  - 静态 golden 已覆盖 validate/roundtrip/参数完整性；
  - 但还没有连续 5/5 真机 Run 记录、性能预算自动判定、异常页面回归。

- **新债 7：平台合规没有因效率提升而放松**：
  - 个人微信 UI 自动化仍必须默认禁用；
  - 企业微信官方 API 仍是唯一合规自动发送路径；
  - 凭证安全、最小权限、调用审计仍需在真实测试号联调前完成。

## 2.19 目标收敛：从一个“能跑的点单宏”到“可复制的个人执行平台”

- 瑞幸 11.7s 证明：**确定性路径可以非常快**。
- FlowScript 证明：**路径可以变成数据、可审核、可版本化、可导入**。
- 但真正的终极目标不是“瑞幸点得快”，而是：
  1. 用户说出/触发目标；
  2. 系统选择已审核的确定性 Skill/FlowScript；
  3. 执行到终端安全态；
  4. 自动断言 + trace + 证据；
  5. 不可逆步骤永远等人工；
  6. 失败能降级、能提醒、能恢复；
  7. 多域复制时不需要重写执行器。


## 3. 稳定架构：避免冲突的分层模型

不要把 LLM、自动化、安全、验证混成一个大循环。应固定为六层：

```text
1. 入口层    Voice / Text / Trigger / Overlay / Wake
            → 统一 GlobalVoiceCommandBus
2. 理解规划层 LLM：理解意图、拆目标、选择 Skill、处理异常
            → 不直接决定精确像素
3. 确定性执行层 UiActionLayer + Skill：
            → tap_by_id / tap_by_text / tap_by_desc / wait / assert
            → 坐标仅兜底
4. 安全门层   TerminalSafetyGate / AgentSafety / Preflight / 人工确认
            → 不可逆操作强制等待
5. 验证层     确定性断言 + 截图 + trace：
            → 前台包名 / 关键UI / 无浮层 / 无最终动作 / WAITING_CONFIRM
6. 审计恢复层 可复放 Skill、失败分类、通知、重试、无障碍自愈、设备矩阵
```

原则：

- **LLM 是“规划者”，不是“执行者”**。
- **Skill 是“确定性知识”的载体**，LLM 只能选择/组合 Skill，不能每次重新发明路径。
- **安全门永远在工具执行之前和终端结束之前**，不依赖模型文本。
- **验证必须可自动判定**，不能靠“看起来差不多”。
- **失败要可恢复**：重试、降级、等待用户、放弃都要有明确状态。
- **单一事实来源**：一个命令管道、一个能力状态、一个 trace、一套验证矩阵。

---

## 4. 后续开发计划（按优先级）

### P0：真机地基与平台合规（当前最高阻塞）
- [x] `AccessibilityKeepAlive` 基础状态机：DISCONNECTED / CONNECTING / CONNECTED / CRASHED
- [x] 模拟器可验证状态机与截屏
- [ ] 真机设备矩阵：MIUI / 模拟器 / 有无 Shizuku / 有无障碍 / 锁屏 / 离线自愈
- [x] 自动恢复脚本完善：`emulator_mock_e2e.sh` 先启动 App 再写回无障碍；`AccessibilityKeepAlive` 自适应重试+指标
- [ ] 个人微信自动化保持默认禁用；真机回归只跑 Luckin + 企业微信官方 API + Settings
- [x] 设置页新增企业微信 API 凭证测试按钮；真实测试号联调仍待凭证
- [ ] 真实 Home Assistant / 海信空调 / 树莓派 / 窗帘设备联调（需要真实硬件与 Token/SSH）

### P0.5：FlowScript 平台闭环（2026-08-30 已部分完成）

- [x] FlowScript JSON 格式 / schema / 版本 / 校验 / 导入导出
- [x] `FlowScriptStore` + PENDING/APPROVED 审核流
- [x] 通用 `run_flow_script` 工具
- [x] 参数模板 `{key}` 与 `InputText` / `Wait` 动作
- [x] 静态 golden 校验（validate / roundtrip / 参数占位符）
- [x] 执行耗时与迭代数回传
- [ ] 真机 golden：连续 5/5 跑通瑞幸 FlowScript
- [ ] 从成功 trace 自动生成候选 FlowScript
- [ ] FlowScript 版本迁移 / 回滚 / 差异对比
- [ ] 每个 FlowScript 的运行历史、成功率、失败分类
- [ ] 用户级参数默认值（门店/饮品/设备/主机）

### P1：完成确定性执行层
- [x] `TapTool / TapTextTool / SwipeTool / PressKeyTool / DismissPopupsTool` 已走 `UiActionLayer`
- [x] `input_text` 自动验证 + read_ui 失败降级 read_screen
- [x] `ReadUiTool` 已完全收敛到 `UiActionLayer`；Overlay 已扩展权限/终端分类
- [x] `ui_assert` 已写入系统提示作为关键路径验证；[ ] 全量自动化断言矩阵
- [x] 浮层识别规则化：PROMO(AD) / PERMISSION / FUNCTIONAL_PICKER / TERMINAL_CONFIRM
- [x] 坐标仅兜底并返回 `coordinateFallback=true` 标记

### P2：沉淀场景 Skill
- [x] 已内置 4 条 `APPROVED + enabled` Skill：瑞幸/企业微信 API/HA/远程验证
- [x] 已内置瑞幸、企业微信官方 API、HA、远程项目各 1 条可复放 Skill（个人微信除外）
- [x] 内置 Skill 已记录每步目的 / 预期 / 验证 / 兜底 / 终端停止点
- [x] 已知路径已由 FlowScript + UiFlowExecutor 做确定性执行；新域仍需沉淀脚本并真机回归

### P3：终端安全矩阵
- [x] 类型扩展：支付 / 消息发送 / 删除 / 配置修改 / HA 安防 / 远程破坏性
- [ ] 每个域补 UI 特征、禁止动作、人工确认 UI、trace 标记
- [ ] 不可逆操作永远不可自动确认
- [ ] 增加“最后一步操作留给用户”的明确 UI 提示

### P3.5：熄屏 / 无 Shizuku 降级与续跑（2026-08-30 已部分完成）

- [x] `VIBRATE` 权限与 `strong_remind` 全屏强提醒工具
- [x] 定时任务失败/等待且熄屏时自动发送强提醒
- [x] `ASSIST / AMBIENT / NOTIFY` 执行模式已定义并暴露
- [ ] 调度器按执行模式主动路由（不是失败后才降级）
- [ ] 用户点亮后自动恢复原任务并继续执行的状态机
- [ ] 无 Shizuku 熄屏任务的“提醒后等待恢复”完整闭环

### P4：严格 E2E 与可观测性
- [x] 模拟器 Mock LLM 回归套件（11/11 通过，含等待确认/弹窗/按键/断言/微信风控/WeCom与HA未配置）
- [x] 自动 trace 报告：路径 / 耗时 / 截图 / Token / 请求体 / 失败原因（`AgentTraceReportBuilder`）；[ ] 真机基线
- [x] 真机 + 模拟器双跑（Mock E2E 14/14，`scripts/dual_device_mock_e2e.sh`）
- [x] 失败自动归类：权限 / 无障碍 / 模型 / UI变化 / 安全拦截 / 平台合规（TraceReportBuilder）
- [x] 瑞幸真实 LLM 耗时基线：198s → 80s → **11.7s / 3 工具调用**；其他域待建基线

### P5：本地语音与长期稳定性
- [ ] 本地 KWS/ASR 1 小时 soak
- [ ] 低电量/灭屏/亮屏策略回归
- [ ] 语音触发 → Agent → 结果 TTS 的端到端验收
- [ ] 唤醒词误唤醒率、识别延迟、功耗数据

### P6：能力扩展
- [ ] 日历、提醒、支付、健康、出行、购物、远程开发、智能家居
- [ ] 在稳定地基上扩展，不反向引入新债

---

## 5. 从类似项目可以借鉴什么

### 5.1 移动端 UI 自动化：Appium / Maestro / UiAutomator
- 借鉴：
  - 以 **resource-id / text / content-desc** 为稳定选择器
  - `waitFor` + `assert` 内建
  - 用 hierarchy + screenshot 双重证据
- 不照搬：
  - 不要求每个 App 都写死坐标
  - 不把测试脚本等同于运行时 Agent

### 5.2 浏览器自动化：Playwright / Cypress
- 借鉴：
  - 可复现 selector、自动等待、失败截图/trace/video
  - 断言明确、重试策略、fixtures
- 不照搬：
  - 手机没有 DOM；我们需要 accessibility tree 等价物
  - 浏览器测试可精确注入，手机受 MIUI/权限限制

### 5.3 RPA：UiPath / Automation Anywhere
- 借鉴：
  - “录制成功路径 → 变成可复用流程”
  - 人工确认步骤、异常处理、审计日志
- 不照搬：
  - 不做大规模后台无人值守的支付/发送
  - 不把“流程完成”误认为“安全完成”

### 5.4 手机自动化 App：Tasker / MacroDroid / Shortcuts
- 借鉴：
  - 触发器 + 动作 + 条件 + 结果验证
  - 用户可理解、可编辑、可关闭
  - 自动化不应吞掉用户的最终控制权
- 不照搬：
  - 它们是固定宏，缺少自然语言理解
  - 我们要用 LLM 做语义入口，但底层仍要像宏一样确定

### 5.5 智能家居：Home Assistant
- 借鉴：
  - entity/domain/service 抽象
  - 安全域设备（锁/摄像头/安防）单独处理
  - 状态读取后验证，不盲发指令
- 不照搬：
  - 智能家居 API 比手机 UI 稳定得多，不能把手机 UI 当作同等可靠 API

### 5.6 计算机使用 Agent：OpenAI Operator / Anthropic Computer Use / Rabbit
- 借鉴：
  - 感知 + 工具 + 安全策略 + 人工确认
  - 不直接裸坐标，优先语义选择器
  - 截图与可访问性树结合
- 不照搬：
  - 它们仍偏向“模型自由探索”
  - 我们要更保守：Skill 驱动 + 确定性验证 + 更强人工兜底

### 5.7 自动驾驶 / 机器人安全
- 借鉴：
  - 分层安全：感知、规划、执行、验证、监控
  - 保守行为：不确定就减速/停下/询问
  - 人类接管机制
- 不照搬：
  - 我们不造车，但“不确定就停在安全态”完全适用

### 5.8 语音助手：Alexa / Google Assistant
- 借鉴：
  - 意图槽位、确认对话、购买/发送前确认
  - 本地唤醒、低功耗、降级
- 不照搬：
  - 不做云端全家桶依赖
  - 我们要把“真正执行到终端态”作为核心价值

### 5.9 DevOps / SRE
- 借鉴：
  - 健康检查、重试、幂等、canary、runbook、postmortem
  - 可观测性：trace、指标、日志、告警
- 不照搬：
  - 不把用户手机当生产集群，但可以把每条 Skill 当 runbook

### 5.10 平台合规 / 账号体系安全（微信、银行、云平台）
- 借鉴：
  - 遵守平台 ToS，不用官方禁止的模拟器/Hook/脚本污染主账号
  - 生产环境使用**官方 API + 专用测试号/沙箱**
  - 风控意识内建：账号分级、频率限制、人工确认、审计
- 不照搬：
  - 不把“小号没有被封”当作合规；个人微信自动化仍应避免成为产品依赖

### 5.11 智能家居/DIY 硬件：Home Assistant / ESPHome / Zigbee2MQTT / VRF
- 借鉴：
  - Home Assistant 的 entity/domain/service 模型，让 Agent 不关心具体品牌和总线；
  - ESPHome 把 ESP32/ESP8266 变成可控本地节点，适合低成本 DIY 窗帘/传感器；
  - Zigbee2MQTT/ZHA 提供本地、稳定的无线协议；
  - VRF/Modbus 网关把海信等中央空调变成标准 `climate` 实体。
- 不照搬：
  - 不把 DIY 电动窗帘当成“已稳定产品”；必须做限位、断电恢复、防夹、手动接管；
  - 不绕过空调厂商总线/强电安全，不鼓励用户私拆高档设备；
  - 不把“能控制”当“有证据”，HA 操作后仍要回读状态验证。

### 5.12 金融/支付系统的“双人确认”与最小权限
- 借鉴：
  - 支付、转账、删除、覆盖等操作必须“双人/人工确认”
  - 最小权限、操作留痕、失败回滚/可恢复
  - 权限与执行分离：Agent 可以准备，但不能独自完成不可逆动作
- 不照搬：
  - 我们不是银行系统，但“最后一步必须人工”的原则完全适用

---

## 6. 明确不做什么

- 不把“生成草稿 / 准备清单 / 定时创建”当端到端通过
- 不依赖 LLM 做精确像素级点击
- 不把“工具调用成功”当“用户目标完成”
- 不绕过不可逆安全门
- 不为了功能数量牺牲稳定性
- 不重复造多套执行器
- 不新增无法验证的“演示型功能”

---

## 7. 成功度量

最终用这组指标判断是否真的在靠近目标：

| 维度 | 指标 |
|---|---|
| 真实完成 | 到达终端确认页/最后一操作前，且由用户实际完成 |
| 可复现 | 同一 Skill 连续 5/5 次走通 |
| 可验证 | 每个场景自动断言全部通过，非人工目测 |
| 安全性 | 不可逆操作 0 次未确认执行 |
| 平台合规 | 个人微信自动化默认禁用；企业微信/官方 API 才允许自动发消息 |
| 可恢复 | 无障碍掉线后自动恢复，或明确提示 |
| 可观测 | 每次都有完整 trace + 截图 + 失败原因 |
| 自然入口 | 语音/文字/触发器都能稳定进入同一管道 |
| 长期稳定 | 本地 KWS/ASR 功耗与唤醒率有数据 |

---

## 8. 下一步 Immediate

1. ✅ 模拟器 Mock LLM E2E 4/4 通过；无障碍状态机、截屏体积、循环拦截、微信风控、企业微信工具均已验证。
2. ✅ 个人微信自动化默认禁用；企业微信官方 API 工具与设置页已落地。
3. ✅ `PressKeyTool / DismissPopupsTool` 收敛到 `UiActionLayer`；`input_text` 有截图证据回退。
4. ✅ `TerminalSafetyGate` 扩展到删除/配置/HA 安防/远程破坏性。
5. 🔜 拿到企业微信测试号，完成官方 API 真实发送联调。
6. ✅ 已内置 4 条 APPROVED Skill，模型提示已改为优先参考技能；下一步接入真实测试号联调。
7. 🔜 真机设备矩阵、无障碍自愈、锁屏/离线处理。
8. 🔜 真机耗时基线 + 自动 trace 报告，目标简单 <10s、常见多步 <60s、复杂 <120s。
9. 🔜 真实 Home Assistant / 树莓派 / 海信空调 / 百褶帘联调，沉淀智能家居 Skill。
10. 🔜 低成本 DIY 智能家居只作为验证路径，不进入“无证据的演示功能”。
11. 之后才扩展更多 App / 能力。
